# 🚀 Руководство по миграции на новую БД Supabase

## ✅ Что было сделано

### 1. Удалена старая БД
- ✅ Удалена папка `supabase/` со всеми миграциями
- ✅ Очищены старые ссылки на прошлый проект

### 2. Создана новая схема БД
- ✅ Файл `database-schema.sql` содержит полную схему для новой БД
- ✅ Включены все таблицы: profiles, teams, tournaments, matches и т.д.
- ✅ Настроены RLS политики для безопасности
- ✅ Добавлены триггеры и функции

### 3. Обновлена система авторизации
- ✅ Создан хук `useAuth` (`src/hooks/useAuth.ts`)
- ✅ Обновлены компоненты входа и регистрации
- ✅ Добавлен `RegisterForm` и `RegisterModal`
- ✅ Обновлён `TopBar` с новой навигацией
- ✅ Все компоненты используют единый `useAuth` хук

---

## 📋 Инструкция по настройке

### Шаг 1: Настройте новый Supabase проект

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш новый проект
3. Перейдите в **SQL Editor**
4. Скопируйте весь код из файла `database-schema.sql`
5. Вставьте и выполните (кнопка **RUN**)

✅ **Результат**: Все таблицы, триггеры, RLS политики и функции будут созданы.

---

### Шаг 2: Обновите переменные окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-anon-key
```

**Где взять эти данные:**
1. В Supabase Dashboard → **Project Settings** → **API**
2. Скопируйте:
   - `Project URL` → в `VITE_SUPABASE_URL`
   - `anon public` ключ → в `VITE_SUPABASE_ANON_KEY`

---

### Шаг 3: Включите Email авторизацию

1. В Supabase Dashboard → **Authentication** → **Providers**
2. Убедитесь, что **Email** включен
3. *(Опционально)* Настройте **Google** и **Discord** OAuth:
   - Включите провайдеров
   - Добавьте Client ID и Client Secret
   - Добавьте Redirect URLs: `https://ваш-проект.supabase.co/auth/v1/callback`

---

### Шаг 4: (Опционально) Обновите TypeScript типы

Если хотите иметь актуальные типы БД:

```bash
npm install -g supabase
supabase login
supabase link --project-ref ваш-project-ref
npx supabase gen types typescript --local > src/integrations/supabase/types.ts
```

**Примечание**: В проекте уже есть рабочие типы, этот шаг опционален.

---

### Шаг 5: Запустите проект

```bash
npm install
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173)

---

## 🎯 Проверка работы

### Тест регистрации:
1. Нажмите **"Регистрация"** в TopBar или перейдите на `/signup`
2. Заполните форму:
   - Email
   - Юзернейм (3-16 символов, латиница/цифры)
   - Пароль (минимум 8 символов)
   - Подтвердите пароль
   - Примите условия
3. Нажмите **"Создать аккаунт"**
4. ✅ Вы должны быть автоматически залогинены и увидеть аватар в TopBar

### Тест входа:
1. Нажмите **"Войти"** в TopBar
2. Введите email или юзернейм + пароль
3. Нажмите **"Войти"**
4. ✅ Вы должны увидеть аватар и токены в TopBar

### Тест выхода:
1. Кликните на аватар в TopBar
2. Выберите **"Выйти"**
3. ✅ Вы должны увидеть кнопки "Войти" и "Регистрация"

---

## 📂 Структура изменений

### Новые файлы:
- ✅ `database-schema.sql` — полная схема БД
- ✅ `src/hooks/useAuth.ts` — хук авторизации
- ✅ `src/components/auth/RegisterForm.tsx` — форма регистрации
- ✅ `src/components/auth/RegisterModal.tsx` — модалка регистрации
- ✅ `MIGRATION-GUIDE.md` — это руководство

### Обновлённые файлы:
- ✅ `src/components/auth/LoginForm.tsx` — использует `useAuth`
- ✅ `src/components/TopBar.tsx` — интегрирован `useAuth`, добавлена модалка регистрации
- ✅ `src/pages/Signup.tsx` — использует `useAuth`

### Удалённые:
- ✅ Папка `supabase/` со старыми миграциями

---

## 🔧 Особенности реализации

### ✅ Supabase клиент (`src/integrations/supabase/client.ts`)
Единая точка входа для всех запросов к БД и auth:
```typescript
import { supabase } from '@/integrations/supabase/client';
```

### ✅ Хук `useAuth`
Предоставляет всё необходимое для работы с авторизацией:
```typescript
const { 
  user,              // User | null
  profile,           // Profile | null
  session,           // Session | null
  loading,           // boolean
  isAuthenticated,   // boolean
  signIn,            // (email, password) => Promise<boolean>
  signUp,            // (email, password, username, data?) => Promise<boolean>
  signOut,           // () => Promise<void>
  signInWithGoogle,  // () => Promise<void>
  signInWithDiscord, // () => Promise<void>
} = useAuth();
```

### ✅ RLS (Row Level Security)
Все таблицы защищены RLS политиками:
- **profiles**: просмотр всем, редактирование только себе
- **teams**: создание/управление только капитаном
- **tournaments**: создание/управление только организатором
- **team_invites/applications**: доступ только участникам

### ✅ Автоматическое создание профиля
При регистрации через `auth.signUp` триггер автоматически создаёт запись в `profiles`:
- `id` = `auth.users.id`
- `username` из meta данных
- Остальные поля можно обновить позже

---

## ⚠️ Важные замечания

1. **Не удаляйте файл `database-schema.sql`** — он может понадобиться для восстановления схемы
2. **Не коммитьте `.env`** — добавьте его в `.gitignore`
3. **После любых изменений схемы БД** рекомендуется обновить TypeScript типы
4. **Email confirmation** по умолчанию отключен в dev-режиме Supabase

---

## 🐛 Troubleshooting

### Проблема: "User already registered"
**Решение**: Проверьте в Supabase Dashboard → Authentication → Users, возможно пользователь уже существует. Удалите или используйте другой email.

### Проблема: "Invalid login credentials"
**Решение**: 
- Убедитесь, что пользователь подтвердил email (если включена верификация)
- Проверьте правильность пароля
- Проверьте, что функция `get_email_by_username` существует в БД

### Проблема: Типы TypeScript не совпадают
**Решение**: Обновите типы командой из Шага 4 или используйте существующие в `src/integrations/supabase/types.ts`

### Проблема: RLS блокирует запросы
**Решение**: 
- Проверьте, что все политики из `database-schema.sql` применены
- Убедитесь, что пользователь авторизован: `supabase.auth.getUser()`

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера на ошибки
2. Проверьте переменные окружения в `.env`
3. Убедитесь, что SQL скрипт выполнен полностью без ошибок
4. Проверьте логи в Supabase Dashboard → Database → Logs

---

## ✨ Готово!

Теперь у вас рабочая система авторизации с новой чистой БД Supabase. Можно создавать пользователей, логиниться и начинать работу с платформой! 🎮

---

**Дата обновления**: 19 ноября 2025  
**Версия**: 1.0.0

